server:
  port: 80
spring:
  cache:
    jcache:
      provider:
  mvc:
    hindered:
      filter:
        enabled: true
  datasource:
    druid:
      driver-class-name: com.mysql.cj.jdbc.Driver
      url: jdbc:mysql://localhost/test?serverTimezone=UTC
      username: root
      password: 123
